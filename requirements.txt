# Core runtime — required for offline / mock mode (everything runs with just these).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
pyyaml>=6.0
beautifulsoup4>=4.12
langgraph>=0.2.0
langchain-core>=0.3.0

# Azure integrations — only needed when you supply real keys.
# Install with:  pip install -r requirements-azure.txt
